# Deployment package

This package composes platform-owned contracts into supported self-hosted topologies. It must
not become a second domain model or encode provider/VPS/hardware identities into canonical
resources.

`single_node.py` is the first production baseline. Future distributed and HA profiles extend
the same canonical contracts rather than replacing it.
