name: Final error-boundary cleanup

on:
  push:
    branches:
      - codex/983-final-error-boundary-audit

permissions:
  contents: write

jobs:
  cleanup:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: codex/983-final-error-boundary-audit
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Apply reviewed classifications
        run: python scripts/maintenance/final_error_boundary_cleanup.py
      - name: Commit classified tree
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No cleanup changes produced."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src
          git commit -m "fix: classify remaining platform error boundaries"
          git push origin HEAD:codex/983-final-error-boundary-audit
