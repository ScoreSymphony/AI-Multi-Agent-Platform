# Frontend

This directory is reserved for the API-first web/control-plane client.

The frontend must consume canonical platform APIs and must not couple directly to concrete orchestrators, executors, model providers or storage backends.
