name: "ScoreSymphony AI-Agent-VPS Forge subsystem"
purpose: "Historical provenance for the former optional execution-only Forge compatibility runtime removed under issue #991."
status: removed
integration_categories:
  - "historical adapter integration"
  - "historical external optional runtime"
  - "reference-only influence"
canonical_upstream: "https://github.com/ScoreSymphony/AI-Agent-VPS"
pinned_revision: "00b821bc94767865457814bf282982ca242a2e10"
verified_license: "MIT"
license_verification_date: "2026-09-03"
last_review_date: "2026-09-13"
platform_adapter_or_boundary: "Removed under #991. The surviving platform-owned Executor contract and ReferenceExecutor remain independent of Forge; this record documents only the former forge-executor-sidecar/v1 integration."
local_source_path: "none; src/ai_multi_agent_platform/adapters/forge.py and forge_http.py were removed under #991"
source_origin_path: "Historical runtime source was AI-Agent-VPS core/forge/crates/executor-sidecar plus reused core/forge/crates/executors, cli-adapters, git and api-types; no upstream source was copied into this repository."
modified_locally: false
modification_summary: "No upstream source was vendored. The former platform-owned Python adapter and HTTP transport were deleted after the #889/#46 removal gates passed."
required_notices:
  - "No upstream source is vendored or copied into this repository. Preserve MIT notices if substantial upstream source is copied in a future, separately reviewed integration."
compatibility_constraints:
  - "No current Forge compatibility claim exists after #991."
  - "Canonical Task/Run/Step/correlation identity and lifecycle ownership remain platform-owned and require no Forge state migration."
  - "Historical persisted/imported provider-private Forge identifiers remain non-canonical metadata and may still be rejected by portability validation."
security_constraints:
  - "No Forge sidecar is started, configured, exposed or authenticated by the current platform."
  - "Historical sidecar security findings are retained only in the #9/#991 audit documents."
deployment_constraints:
  - "Forge is not an available deployment profile and is not required for baseline, optional conformance or release CI."
resource_constraints:
  - "No current Forge runtime resources are required."
required_for_baseline: false
recurring_paid_service_required: false
update_review_method: "Historical record only. Do not update the old pin for routine upstream changes. Any future Forge reintroduction requires a new adoption/support decision, current provenance/security review and fresh conformance evidence."
exit_replacement_strategy: "Completed under #991: executable adapter, transport, Forge-only tests, sidecar CI and active support/release claims were removed. ReferenceExecutor remains the migration target; canonical state required no migration."
adr: "docs/adr/0013-deprecate-forge-execution-adapter-pending-removal-gates.md"
notes: "Issue #9 established the former execution-only boundary. Issue #991 first deprecated it, then removed the executable integration after #889 and #46 proved the required backend-neutral/reference guarantees. This file is retained solely as historical architecture, license and provenance evidence."
