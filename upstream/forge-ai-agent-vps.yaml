name: "ScoreSymphony AI-Agent-VPS Forge subsystem"
purpose: "Historical provenance for the removed Forge execution adapter/sidecar integration and for recovery, event/idempotency and workspace reuse lessons recorded under issues #9 and #991."
status: removed
integration_categories:
  - "historical adapter integration"
  - "reference-only influence"
canonical_upstream: "https://github.com/ScoreSymphony/AI-Agent-VPS"
pinned_revision: "00b821bc94767865457814bf282982ca242a2e10"
verified_license: "MIT"
license_verification_date: "2026-09-03"
last_review_date: "2026-09-13"
platform_adapter_or_boundary: "No executable Forge adapter remains. The canonical platform Executor contract, lifecycle authority, Node/Worker runtime and ReferenceExecutor remain platform-owned; the removed integration had implemented Executor through a platform-owned ForgeClient/HTTP sidecar boundary."
local_source_path: "docs/integrations/FORGE_RETENTION_DECISION.md; docs/integrations/FORGE_REUSE_AUDIT.md; docs/integrations/FORGE_ADAPTER.md"
source_origin_path: "Historical source: AI-Agent-VPS core/forge/crates/executor-sidecar plus reused core/forge/crates/executors, cli-adapters, git and api-types; no upstream source was copied into this repository."
modified_locally: false
modification_summary: "No upstream Forge source is vendored. The former platform-owned Python adapter/HTTP client and pinned sidecar CI consumption were removed under #991 after ADR 0013 gates passed; only provenance and architecture history remain."
required_notices:
  - "No upstream source is vendored or copied into this repository. The upstream runtime retains its MIT license metadata at its source repository. Preserve MIT notices if substantial upstream source is ever copied locally in a future independently reviewed integration."
compatibility_constraints:
  - "There is no active Forge runtime or compatibility claim after #991 / ADR 0014."
  - "The pinned revision and forge-executor-sidecar/v1 protocol are retained only as historical provenance for the removed integration."
  - "Forge Task/Project/Event/API/DB types were never canonical platform types."
  - "Canonical Task/Run/Step/correlation identity, retry policy, lifecycle and event ownership remain in the platform."
  - "Reintroducing Forge requires a new explicit support decision with non-null execution evidence meeting #904; the historical null-sidecar evidence is insufficient."
security_constraints:
  - "No Forge process, listener, workspace mapping or credential boundary is active in the platform after removal."
  - "Historical sidecar isolation and allowlist properties must not be interpreted as present platform controls."
deployment_constraints:
  - "Forge is not installed, configured, checked out, built or started by supported platform deployment/CI paths."
  - "No supported deployment depends on AI-Agent-VPS Forge artifacts."
resource_constraints:
  - "No runtime resources are allocated to Forge by the platform after removal."
required_for_baseline: false
recurring_paid_service_required: false
update_review_method: "No runtime update tracking is required for the removed integration. Review this record only when correcting provenance/history or evaluating a new independently approved Forge reintroduction proposal."
exit_replacement_strategy: "Completed under #991: the executable adapter, transport, Forge-only tests, sidecar CI lane and active compatibility claims were removed. ReferenceExecutor and the generic Node/Worker/runtime architecture remain the baseline; canonical state required no migration."
adr: "docs/adr/0014-remove-forge-execution-adapter-after-removal-gates.md"
notes: "Historical record only. Issue #9 established the execution-only boundary; ADR 0013 demoted it to deprecated while removal gates were gathered; #889/#46 and backend-neutral evidence then satisfied those gates; ADR 0014/#991 removed the executable integration. The original pinned source remains recorded so the reuse history is auditable."
