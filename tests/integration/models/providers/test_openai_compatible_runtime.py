"""OpenAI-compatible provider/runtime integration coverage originating in issue #10."""

import openai_provider_cases as cases

test_runtime_routes_canonical_model_then_provider_resolves_native_name = (
    cases.test_runtime_routes_canonical_model_then_provider_resolves_native_name
)
