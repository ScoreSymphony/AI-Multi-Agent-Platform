# Adapters

This directory is reserved for integration-specific packaging, assets or components that implement platform-owned contracts.

Concrete adapter contracts are intentionally not defined during bootstrap. They will be specified by the numbered architecture issues.
