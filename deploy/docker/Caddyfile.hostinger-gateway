{
    admin off
    auto_https off
}

http://{$AI_MAP_PUBLIC_DOMAIN}:8080 {
    reverse_proxy web:8080

    header {
        X-Content-Type-Options nosniff
        Referrer-Policy no-referrer
        -Server
    }
}
