{
    admin off
}

{$AI_MAP_PUBLIC_DOMAIN} {
    encode zstd gzip

    reverse_proxy web:8080

    header {
        X-Content-Type-Options nosniff
        Referrer-Policy no-referrer
        -Server
    }
}
